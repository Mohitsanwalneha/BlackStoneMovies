@model IEnumerable<BlackStoneMovies.Models.Movy>

@{
    ViewBag.Title = "AllMovies";
}

<h2>AllMovies</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MDirector)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MProducer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MDirector)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MProducer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MId })
        </td>
    </tr>
}

</table>
